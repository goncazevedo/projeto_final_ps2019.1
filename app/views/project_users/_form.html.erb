<%= form_with(model: project_user, local: true) do |form| %>
  <% if project_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_user.errors.count, "error") %> prohibited this project_user from being saved:</h2>

      <ul>
      <% project_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :project_id, value: @project.id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select(:user_id, User.all, :id, :name, { :prompt => 'Escolha um Membro', :selected => @project_user.user_id } )%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
