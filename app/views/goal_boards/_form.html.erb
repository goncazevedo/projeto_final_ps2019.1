<%= form_with(model: goal_board, local: true) do |form| %>
  <% if goal_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal_board.errors.count, "error") %> prohibited this goal_board from being saved:</h2>

      <ul>
      <% goal_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :deadline %>
    <%= form.date_select :deadline %>
  </div>

  <div class="field">
    <%= form.label :finished %>
    <%= form.check_box :finished %>
  </div>

  <div class="field">
    <%= form.hidden_field :board_id, value: @board.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
